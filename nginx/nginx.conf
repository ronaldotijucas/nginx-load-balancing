worker_processes 4;

events {
  worker_connections 1024;
}

http {
  upstream upstream_server {
    server api:8000;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://upstream_server/$is_args$args;
    }
  }
}
